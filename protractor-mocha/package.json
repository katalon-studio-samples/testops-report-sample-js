{
  "name": "testops-protractor-sample",
  "version": "1.0.0",
  "description": "TestOps Protractor sample",
  "scripts": {
    "build": "tsc",
    "pretest": "npm run cleanup && npm run build && npm run update-webdriver",
    "test": "protractor",
    "cleanup": "rimraf dist",
    "update-webdriver": "webdriver-manager update"
  },
  "author": "Katalon, LLC. (https://www.katalon.com)",
  "license": "ISC",
  "dependencies": {
    "@katalon/testops-mocha": "^1.2.1",
    "@katalon/testops-protractor": "^1.0.0",
    "chai": "^4.3.4",
    "chai-as-promised": "^7.1.1"
  },
  "devDependencies": {
    "@tsconfig/node10": "^1.0.7",
    "@types/chai": "^4.2.14",
    "@types/chai-as-promised": "^7.1.3",
    "mocha": "^8.2.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.1.3"
  }
}
